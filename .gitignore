# ============================================================
# ข้อมูล — ห้ามอัปขึ้น git เด็ดขาด (ข้อมูลผู้ป่วยจริง + ไฟล์ใหญ่มาก)
# ============================================================
data/raw/
data/interim/
data/processed/

# ============================================================
# ผลการเทรน — generate ใหม่ได้จาก config + โค้ด ไม่ต้องเก็บใน git
# (ไฟล์ weight เดียว (.pt) มักใหญ่หลายร้อย MB เกิน limit ปกติของ git)
# ============================================================
outputs/

# ============================================================
# Python มาตรฐาน
# ============================================================
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg
build/
dist/

# ============================================================
# Virtual environment
# ============================================================
.venv/
venv/
env/

# ============================================================
# Jupyter (ถ้าใช้ notebook เพิ่มเติมนอกจากไฟล์สรุป .md)
# ============================================================
.ipynb_checkpoints/

# ============================================================
# Cache ของ library ที่โค้ดในโปรเจกต์นี้ใช้
# ============================================================
.cache/
*.cache

# ============================================================
# IDE / Editor
# ============================================================
.vscode/
.idea/
*.swp

# ============================================================
# OS
# ============================================================
.DS_Store
Thumbs.db

# ============================================================
# Secret / config เฉพาะเครื่อง (ถ้ามีในอนาคต เช่น HF_TOKEN)
# ============================================================
.env
*.env.local

# ============================================================
# ข้อมูลผู้ป่วยจริงที่วางไว้ที่ root — ห้ามอัปขึ้น git เด็ดขาด
# (บล็อกด้านบนครอบแค่ data/ แต่ข้อมูลจริงบนเครื่องนี้อยู่ที่ root)
# ============================================================
PNG/
VFA/
masks/
crops/
splits/

# กันพลาดอีกชั้น — ไฟล์ประเภทนี้ไม่ควรขึ้น git ไม่ว่าจะอยู่ที่ไหน
*.xlsx
*.dcm
*.png
*.pt

# ยกเว้นภาพประกอบเอกสาร (ถ้าวันหนึ่งจะใส่) ให้ปลดล็อกเป็นรายไฟล์แบบนี้
# !docs/example.png
